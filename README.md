# 路線偏離測試器 v6

新增重點：

1. 支援 GPX / KML。
2. 路線匯入後會自動保存到 iPhone 本機 localStorage。
3. 頁面被關閉或 PWA 重啟後，會自動恢復上次路線。
4. 返回正確路徑時會提醒「已回到正確路徑」。
5. 偏離時新增返回提示，例如正確路線在左方、右方、前方、後方，或可能走反。
6. 保留 GPS 品質診斷；GPS 不可靠時會暫停方向提示與大聲偏離警報。

覆蓋 GitHub repo 檔案後，建議刪除 iPhone 主畫面的舊 PWA 圖示，再重新加入主畫面，避免舊版 Service Worker 快取。
